#include <iostream>
#include <string>
using namespace std;

int main(){
    string s;
    getline(cin, s);

    char c;
    cin >> c;

    string r = "";
    for(char x : s)
        if(x != c)
            r += x;

    cout << r;
    return 0;
}
